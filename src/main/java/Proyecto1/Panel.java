package Proyecto1;

import javax.swing.*;
import java.awt.*;

public class Panel extends JPanel {
    public Panel(){
        setLayout(null);
        setSize(400,400);
        setBackground(Color.green);
    }
}
